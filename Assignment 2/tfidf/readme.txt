TF-IDF implementation:-

Requirements:-
1) Python version:- 2.7
2) requests python library required
3) keep english.txt ,TF-IDFImplementation.py,tfidf.py in one folder.

Changes in code to implement:-
1) set url in equests.get function in TF-IDFImplementation.py to correct ip address where solr index is running.

To run program:-
1) Once you have done above changes and have necessary tools,go to location from terminal where the tfidf folder is stored.
3) Type command:-  python TF-IDFImplementation.py